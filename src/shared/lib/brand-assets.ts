import brandLogo from '@/assets/logotherapy.png';

/** Nome oficial da plataforma (exibido em títulos, alt de logos, PDFs, push). */
export const BRAND_NAME = 'Unithery';

/** URL resolvida pelo Vite no build (evita path quebrado em produção/Vercel). */
export const BRAND_LOGO_SRC = brandLogo;
